import React, { Fragment } from "react";
import { Helmet } from "react-helmet-async";

const Home = props => {
  return (
    <Fragment>
      <Helmet title="Sander de Bruijn" />
    </Fragment>
  );
};

export default Home;
